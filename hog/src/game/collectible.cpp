#include <game/collectible.hpp>
#include <klib/assert.hpp>

namespace hog {
// auto Collectible::check_collect(glm::vec2 const cursor) -> bool {
// 	KLIB_ASSERT(prop != nullptr);
// 	if (!collected && prop->sprite.bounding_rect().contains(cursor)) {
// 		collected = true;
// 		return true;
// 	}
// 	return false;
// }
} // namespace hog
